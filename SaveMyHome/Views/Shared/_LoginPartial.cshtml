@using Microsoft.AspNet.Identity
@using Resources
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", new { area =""}, FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
    @Html.AntiForgeryToken()

    <ul class="nav navbar-nav">
        <li>
            @Html.ActionLink($"{LoginPartial.Hello} {User.Identity.GetUserName()}!", "Index", new { area = "", controller = "Manage", title = "Manage" })
        </li>
        <li><a href="javascript:document.getElementById('logoutForm').submit()">@LoginPartial.Logoff</a></li>
    </ul>
    }
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink(LoginPartial.Register, "Register", new { area = "", controller = "Account", id = "registerLink" })</li>
        <li>@Html.ActionLink(LoginPartial.Login, "Login", new { area = "", controller = "Account", id = "loginLink" })</li>
    </ul>
}
